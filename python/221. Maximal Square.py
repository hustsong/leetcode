from typing import List


class Solution:
    def maximalSquare(self, matrix: List[List[str]]) -> int:
        side = 0
        m, n = len(matrix), len(matrix[0])

        for i in range(n):
            row_sums = [0] * m
            for j in range(i, n):
                ones = 0
                continuous = 0
                for k in range(m):


                if :
                    break

        return side * side


sol = Solution()
